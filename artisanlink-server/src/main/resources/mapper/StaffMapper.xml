<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.johan.artisanlink.server.mapper.StaffMapper">

    <!-- 员工分页查询 -->
    <select id="pageQuery" resultType="com.johan.artisanlink.pojo.po.Staff">
        SELECT
            id,
            merchant_id,
            name,
            phone,
            position,
            entry_time,
            skill_tags,
            avatar,
            status,
            remark,
            create_time,
            update_time
        FROM staff
        <where>
            <if test="merchantId != null">
                AND merchant_id = #{merchantId}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="position != null and position != ''">
                AND position = #{position}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
